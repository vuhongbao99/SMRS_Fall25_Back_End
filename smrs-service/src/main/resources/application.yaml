Spring:
  application:
    name: smrs-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 12345
    url: jdbc:mysql://localhost:3306/smrs

  jpa:
    hibernate:
      ddl-auto: update

# JWT Configuration
jwt:
  secret: very-important-key-that-is-at-least-256-bits-long-for-security-purposes
  expiration: 86400000 # 24 hours in milliseconds
